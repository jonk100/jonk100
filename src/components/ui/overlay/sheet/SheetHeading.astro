---
import ModalHeading from '../../modal/ModalHeading.astro';
export interface Props { class?: string; [key: string]: any; }
const { class: className = '', ...rest } = Astro.props;
---
<ModalHeading class:list={['sheet-heading', className]} {...rest}>
  <slot />
</ModalHeading>
