---
import ModalSubheading from '../../modal/ModalSubheading.astro';
export interface Props { class?: string; [key: string]: any; }
const { class: className = '', ...rest } = Astro.props;
---
<ModalSubheading class:list={['sheet-subheading', className]} {...rest}>
  <slot />
</ModalSubheading>
