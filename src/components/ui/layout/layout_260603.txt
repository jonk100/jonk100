--- File: Box.astro ---
---
/**
 * src/components/ui/layout/Box.astro
 * A foundational layout primitive for applying spacing, margins, display, and animation properties.
 * - Supports responsive padding and margin via spacing scale values
 * - Configurable HTML tag rendering with the 'as' prop
 * @props (local): as, padding, margin, display, animate
 * @props (global): class, class:list, frame, effects
 * - padding: Applies padding using a spacing scale value
 * - margin: Applies margin using a spacing scale value
 * - frame: Applies a specific frame style variant to the component
 * - effects: Applies single or multiple image effects to the component
 * - animate: Applies an entrance or loop animation variant (defined in src/styles/variants/animations.css)
 */
import type { SpacingScale, FrameVariant, ComponentEffect, ComponentRadius, AnimationType } from 'src/types/variants.types.ts';

export interface Props {
  as?: string;
  padding?: SpacingScale;
  margin?: SpacingScale;
  display?: 'block' | 'inline-block' | 'inline' | 'flex' | 'grid' | 'none';
  layer?: -1 | 0 | 1 | 2 | 3 | 4 | 5;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  frame?: FrameVariant;
  effects?: ComponentEffect | ComponentEffect[];
  radius?: ComponentRadius;
  animate?: AnimationType;
  [key: string]: any;
}

const {
  as: Tag = 'div',
  padding,
  margin,
  display,
  layer,
  class: className = '',
  "class:list": classList = {},
  frame,
  effects,
  radius,
  animate = 'none',
  style = '',
  ...rest
} = Astro.props;

const internalStyle = display ? `display: ${display};` : '';
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<Tag
  class:list={[
    'box',
    padding && `p-${padding}`,
    margin && `m-${margin}`,
    layer !== undefined && `layer--${layer}`,
    animate !== 'none' ? `animate-${animate}` : '',
    className,
    classList,
    frame,
    effects,
    radius && `radius--${radius}`
  ]}
  style={combinedStyle}
  {...rest}
>
  <slot />
</Tag>

<style>
  /* Box is purely functional, delegating sizing to global space utilities */
  .box {
    /* Base wrapper properties if needed */
  }
</style>


--- File: Stack.astro ---
---
/**
 * src/components/ui/layout/Stack.astro
 * A vertical layout primitive for stacking elements in a column.
 * - Controls the alignment and spacing of stacked children
 * - Optionally injects horizontal dividers between sibling elements
 * @props (local): as, gap, align, justify, divider
 * @props (global): class, class:list
 * - gap: Determines the spacing between stacked elements
 * - divider: Conditionally adds a border-top between sibling elements via lobotomized owl selector
 */
import type { FlexboxProps } from 'src/types/variants.types.ts';

export interface Props extends FlexboxProps {
  as?: string;
  divider?: boolean;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'div',
  gap = 'md',
  align = 'stretch',
  justify = 'start',
  divider = false,
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

const internalStyle = `align-items: ${align}; justify-content: ${justify};`;
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<Tag
  class:list={[
    'stack',
    `gap-${gap}`,
    { 'stack--divided': divider },
    className,
    classList
  ]}
  style={combinedStyle}
  {...rest}
>
  <slot />
</Tag>

<style>
  .stack {
    display: flex;
    flex-direction: column;
  }
  
  /* Lobotomized owl selector for generic sibling borders */
  .stack--divided > :global(* + *) {
    border-top: 1px solid var(--border);
    padding-top: calc(var(--gap-value, 1rem) / 2);
  }
</style>


--- File: Inline.astro ---
---
/**
 * src/components/ui/layout/Inline.astro
 * A layout primitive for aligning child elements horizontally.
 * - Handles flexbox alignment, justification, and wrapping
 * - Standardizes gap spacing using the design system scale
 * @props (local): as, gap, align, justify, wrap
 * @props (global): class, class:list
 * - gap: Sets the flex gap between inline items
 */
import type { FlexboxProps } from 'src/types/variants.types.ts';

export interface Props extends FlexboxProps {
  as?: string;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'div',
  gap = 'md',
  align = 'center',
  justify = 'start',
  wrap = true,
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

const internalStyle = `
    align-items: ${align};
    justify-content: ${justify};
    flex-wrap: ${wrap ? 'wrap' : 'nowrap'};
  `;
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<Tag
  class:list={[
    'inline',
    `gap-${gap}`,
    className,
    classList
  ]}
  style={combinedStyle}
  {...rest}
>
  <slot />
</Tag>

<style>
  .inline {
    display: flex;
    flex-direction: row;
  }
</style>


--- File: Grid.astro ---
---
/**
 * src/components/ui/layout/Grid.astro
 * A flexible layout primitive for constructing CSS grids.
 * - Supports specific column counts or auto-fitting grid layouts
 * - Utilizes design system spacing scales for gaps
 * @props (local): as, cols, gap, autoFit, minChildWidth
 * @props (global): class, class:list
 * - gap: Dictates the gap between grid items based on the spacing scale
 * - autoFit: Enables dynamic column counting that fits to the container based on minChildWidth
 */
import type { SpacingScale } from 'src/types/variants.types.ts';

export interface Props {
  as?: string;
  cols?: number | string;
  gap?: SpacingScale;
  autoFit?: boolean;
  minChildWidth?: string;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'div',
  cols,
  gap = 'md',
  autoFit = false,
  minChildWidth = '250px',
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

// Determine grid-template-columns logic
let gridTemplateColumns = '';
if (autoFit) {
  gridTemplateColumns = `repeat(auto-fit, minmax(${minChildWidth}, 1fr))`;
} else if (cols) {
  gridTemplateColumns = `repeat(${cols}, minmax(0, 1fr))`;
}

const internalStyle = gridTemplateColumns ? `grid-template-columns: ${gridTemplateColumns};` : '';
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<Tag
  class:list={[
    'grid',
    `gap-${gap}`,
    className,
    classList
  ]}
  style={combinedStyle}
  {...rest}
>
  <slot />
</Tag>

<style>
  .grid {
    display: grid;
  }
</style>


--- File: Center.astro ---
---
/**
 * src/components/ui/layout/Center.astro
 * Centers content along a specified axis or both axes.
 * - Supports both block and inline display types
 * - Allows restriction of centering to horizontal or vertical axis exclusively
 * @props (local): as, inline, axis
 * @props (global): class, class:list
 * - axis: Determines whether centering applies to 'both', 'horizontal', or 'vertical' axes
 */
export interface Props {
  as?: string;
  inline?: boolean;
  axis?: 'both' | 'horizontal' | 'vertical';
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'div',
  inline = false,
  axis = 'both',
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

const styles: Record<string, string> = {};

if (axis === 'both') {
  styles['place-content'] = 'center';
  styles['place-items'] = 'center';
} else if (axis === 'horizontal') {
  styles['justify-content'] = 'center';
  styles['justify-items'] = 'center';
} else if (axis === 'vertical') {
  styles['align-content'] = 'center';
  styles['align-items'] = 'center';
}

const internalStyle = Object.entries(styles)
  .map(([prop, val]) => `${prop}: ${val};`)
  .join(' ');

const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<Tag
  class:list={[
    'center',
    { 'center--inline': inline },
    className,
    classList
  ]}
  style={combinedStyle}
  {...rest}
>
  <slot />
</Tag>

<style>
  .center {
    display: grid;
  }
  .center--inline {
    display: inline-grid;
  }
</style>


--- File: VisuallyInteractive.astro ---
---
/**
 * src/components/ui/layout/VisuallyInteractive.astro
 * Provides an interactive wrapper element that handles focus and active scaling states.
 * - Supports custom rendering tag via 'as' prop.
 * - Features customizable active scaling and hover glow effects.
 * @props (local): as, scale, glow
 * @props (global): class, class:list
 * - as: HTML tag or component to render as the wrapper (e.g., 'button', 'a').
 * - scale: Determines if the component scales down when active.
 * - glow: Enables a glowing box-shadow effect on hover.
 */
export interface Props {
  as?: string;
  scale?: boolean;
  glow?: boolean;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'div',
  scale = true,
  glow = false,
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---

<Tag
  class:list={[
    'interactive-wrapper',
    { 'interactive-wrapper--scale': scale },
    { 'interactive-wrapper--glow': glow },
    className,
    classList
  ]}
  tabindex="0"
  {...rest}
>
  <slot />
</Tag>

<style>
  .interactive-wrapper {
    display: inline-block;
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
    outline: none; /* We handle focus via focus-visible */
  }

  /* Keyboard focus states */
  .interactive-wrapper:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
    border-radius: inherit;
  }

  /* Active scaling physics */
  .interactive-wrapper--scale:active {
    transform: scale(0.96);
  }

  /* Optional hover glow */
  .interactive-wrapper--glow:hover {
    box-shadow: 0 0 30px rgba(var(--accent-rgb), 0.6);
  }
</style>


--- File: BentoGrid.astro ---
---
/**
 * src/components/ui/layout/BentoGrid.astro
 * A bento-box style CSS grid layout primitive.
 * @props (local): gap
 * @props (global): class, class:list
 * - gap: Spacing scale for the grid gap
 */
import type { SpacingScale } from 'src/types/variants.types';

export interface Props {
  gap?: SpacingScale;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  gap = 'md',
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---

<div class:list={['bento-grid', `gap-${gap}`, className, classList]} {...rest}>
  <slot />
</div>

<style>
  .bento-grid {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
  }

  @media (max-width: 900px) {
    .bento-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 580px) {
    .bento-grid {
      grid-template-columns: 1fr;
    }
  }
</style>


--- File: BentoCell.astro ---
---
/**
 * src/components/ui/layout/BentoCell.astro
 * A cell within a BentoGrid, controlling column span based on the bento layout logic.
 * @props (local): span
 * @props (global): class, class:list
 * - span: Dictates the size and layout of the cell
 */
export interface Props {
  span?: 'flagship' | 'secondary' | 'secondary-alt' | 'wide' | 'auto';
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  span = 'auto',
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---

<div class:list={['bento-cell', span !== 'auto' ? `span--${span}` : '', className, classList]} {...rest}>
  <slot />
</div>

<style>
  .span--flagship { grid-column: 1 / 8; }
  .span--secondary { grid-column: 8 / 13; }
  .span--secondary-alt { grid-column: 1 / 6; }
  .span--wide { grid-column: 6 / 13; }

  /* Responsive Resets */
  @media (max-width: 900px) {
    .span--flagship, .span--secondary, .span--secondary-alt, .span--wide {
      grid-column: auto;
    }
  }
</style>


--- File: Container.astro ---
---
/**
 * src/components/Container.astro
 * Container wrapper component.
 * - Delegates class merging to Astro's native class:list system
 * @props (local): as
 * @props (global): class, class:list, frame, effects
 * - as: HTML tag to render
 */

import type { FrameVariant, ComponentEffect } from '/src/types/variants.types';

export interface Props {
  as?: string;
  frame?: FrameVariant;
  effects?: ComponentEffect | ComponentEffect[];
  class?: string;
  "class:list"?: Record<string, boolean>;
  [key: string]: any;
}

const {
  as = "div",
  frame,
  effects,
  class: cls = "",
  "class:list": classList = {},
  ...rest
} = Astro.props;

const Tag = as || "div";

const effectClasses = Array.isArray(effects) 
  ? effects.map(e => `effect--${e}`).join(' ') 
  : effects ? `effect--${effects}` : '';

const classes = [
  "container", 
  frame && `frame-variant--${frame}`, 
  effectClasses, 
  cls
]
  .filter(Boolean)
  .join(" ");
---

<Tag class={classes} class:list={classList} {...rest}>
  <slot />
</Tag>

<style>
  .container {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    padding: var(--space-4);
  }

  .container.sm {
    padding: var(--space-1)  var(--space-2);
  }

  .container.md {
    padding: var(--space-2)  var(--space-3);
  }

  .container.lg {
    padding: var(--space-3) var(--space-4);
  }
  .container.xl {
    padding: var(--space-4) var(--space-8);
  }
  .container.icon {
    padding: var(--space-2);
  }

  .container.icon-sm {
    padding: var(--space-1);
  }

  .container.icon-lg {
    padding: var(--space-3);
  }

  .container.default {
    background-color: var(--bg-surface);
    color: var(--text);
  }

  .container.primary {
</style>


--- File: Divider.astro ---
---
/**
 * src/components/Divider.astro
 * Divider / horizontal rule component.
 * - Delegates class merging to Astro's native class:list system
 * @props (local): as
 * @props (global): class, class:list, frame, effects
 * - as: HTML tag to render
 */

export interface Props {
  as?: string;
  class?: string;
  "class:list"?: Record<string, boolean>;
  [key: string]: any;
}

const {
  as = "hr",
  class: cls = "",
  "class:list": classList = {},
  ...rest
} = Astro.props;

const Tag = as || "hr";

const classes = ["divider", cls]
  .filter(Boolean)
  .join(" ");
---

<Tag class={classes} class:list={classList} {...rest} />


--- File: SrOnly.astro ---
---
/**
 * src/components/ui/layout/SrOnly.astro
 * Visually hides content while keeping it accessible to screen readers.
 * - Provides text context for assistive technologies without breaking visual layouts
 * - Optionally allows content to become visible when focused
 * @props (local): as, focusable
 * @props (global): class, class:list
 * - focusable: Determines if the hidden element should become visible when it receives focus
 */
export interface Props {
  as?: string;
  focusable?: boolean;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  as: Tag = 'span',
  focusable = false,
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---

<Tag
  class:list={[
    'sr-only',
    { 'sr-only-focusable': focusable },
    className,
    classList
  ]}
  {...rest}
>
  <slot />
</Tag>

<style>
  /* Base .sr-only is defined globally in layout.css */
  
  .sr-only-focusable:focus,
  .sr-only-focusable:focus-within,
  .sr-only-focusable:active {
    position: static;
    width: auto;
    height: auto;
    overflow: visible;
    clip: auto;
    white-space: normal;
  }
</style>


--- File: Columns.astro ---
---
/**
 * src/components/ui/layout/Columns.astro
 * A flex/grid wrapper that automatically sizes children into columns.
 * @props (local): layout, gap
 * @props (global): class, class:list
 * - layout: A string of numbers representing fractional column widths (e.g. "121" -> 1fr 2fr 1fr).
 *   If omitted, all children automatically receive equal width.
 */
import type { SpacingScale } from '/src/types/variants.types';

export interface Props {
  layout?: string | number;
  gap?: SpacingScale;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  layout,
  gap = 'md',
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

let gridTemplateColumns;

if (layout) {
  // Convert "121" -> "1fr 2fr 1fr"
  const chars = String(layout).split('');
  gridTemplateColumns = chars.map(char => `${char}fr`).join(' ');
}

const internalStyle = gridTemplateColumns ? `grid-template-columns: ${gridTemplateColumns}` : '';
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<div 
  class:list={['columns-wrapper', `gap-${gap}`, className, classList]} 
  style={combinedStyle}
  {...rest}
>
  <slot />
</div>

<style>
  .columns-wrapper {
    display: grid;
    /* If no layout is provided, auto-flow creates equal-width columns for all children */
    grid-auto-flow: column;
    grid-auto-columns: minmax(0, 1fr);
  }

  .gap-xs { gap: var(--space-2); }
  .gap-sm { gap: var(--space-4); }
  .gap-md { gap: var(--space-6); }
  .gap-lg { gap: var(--space-8); }
  .gap-xl { gap: var(--space-12); }
</style>


--- File: C.astro ---
---
/**
 * src/components/ui/layout/C.astro
 * A semantic column child for the Columns component.
 * @props (global): class, class:list
 */
export interface Props {
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---

<div class:list={['column', className, classList]} {...rest}>
  <slot />
</div>

<style>
  .column {
    display: flex;
    flex-direction: column;
  }
</style>


--- File: Bento.astro ---
---
/**
 * src/components/ui/layout/Bento.astro
 * A specialized grid wrapper for creating asymmetrical "Bento Box" layouts.
 * - Leverages CSS Grid for precise cell positioning
 * - Supports responsive column counts
 * @props (local): columns, gap
 * @props (global): class, class:list
 */
import type { SpacingScale } from 'src/types/variants.types';

export interface Props {
  columns?: number | string;
  gap?: SpacingScale;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  columns = 4,
  gap = 'md',
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

const gridTemplateColumns = typeof columns === 'number' 
  ? `repeat(${columns}, minmax(0, 1fr))` 
  : columns;

const internalStyle = `grid-template-columns: ${gridTemplateColumns};`;
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<div 
  class:list={['bento-grid', `gap-${gap}`, className, classList]} 
  style={combinedStyle}
  {...rest}
>
  <slot />
</div>

<style>
  .bento-grid {
    display: grid;
    /* Bento layouts often benefit from grid-auto-rows to maintain rhythm */
    grid-auto-rows: minmax(100px, auto);
    width: 100%;
  }

  .gap-xs { gap: var(--space-2); }
  .gap-sm { gap: var(--space-4); }
  .gap-md { gap: var(--space-6); }
  .gap-lg { gap: var(--space-8); }
  .gap-xl { gap: var(--space-12); }

  @media (max-width: 768px) {
    .bento-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    }
  }

  @media (max-width: 480px) {
    .bento-grid {
      grid-template-columns: 1fr !important;
    }
  }
</style>


--- File: BentoItem.astro ---
---
/**
 * src/components/ui/layout/BentoItem.astro
 * A semantic child for the Bento grid system.
 * @props (local): colSpan, rowSpan
 * @props (global): class, class:list
 * - colSpan: Number of columns this item should occupy
 * - rowSpan: Number of rows this item should occupy
 */
export interface Props {
  colSpan?: number;
  rowSpan?: number;
  class?: string;
  "class:list"?: Record<string, boolean> | any[];
  [key: string]: any;
}

const {
  colSpan = 1,
  rowSpan = 1,
  class: className = '',
  "class:list": classList = {},
  style = '',
  ...rest
} = Astro.props;

const internalStyle = `grid-column: span ${colSpan}; grid-row: span ${rowSpan};`;
const combinedStyle = [internalStyle, typeof style === 'string' ? style : Object.entries(style || {}).map(([k, v]) => `${k}:${v}`).join(';')].filter(Boolean).join(';');
---

<div 
  class:list={['bento-item', className, classList]} 
  style={combinedStyle}
  {...rest}
>
  <slot />
</div>

<style>
  .bento-item {
    display: flex;
    flex-direction: column;
    height: 100%;
    width: 100%;
  }

  @media (max-width: 768px) {
    .bento-item {
      grid-column: span 1 !important;
      grid-row: span 1 !important;
    }
  }
</style>


--- File: Spacer.astro ---
---
/**
 * src/components/ui/layout/Spacer.astro
 * Token-mapped empty space component, axis-aware and responsive.
 */
import type { SpacingScale } from '/src/types/variants.types';

export interface Props {
  size?: SpacingScale;
  axis?: 'x' | 'y' | 'both';
  class?: string;
  "class:list"?: any;
  [key: string]: any;
}

const {
  size = 'md',
  axis = 'y',
  class: className = '',
  "class:list": classList = {},
  ...rest
} = Astro.props;
---
<div
  class:list={['spacer', `spacer--axis-${axis}`, className, classList]}
  style={`--spacer-size: var(--gap-${size}, 1rem);`}
  aria-hidden="true"
  {...rest}
></div>

<style>
  .spacer {
    display: block;
    flex-shrink: 0;
  }
  .spacer--axis-y {
    height: var(--spacer-size);
    width: 100%;
  }
  .spacer--axis-x {
    width: var(--spacer-size);
    height: 100%;
    display: inline-block;
  }
  .spacer--axis-both {
    width: var(--spacer-size);
    height: var(--spacer-size);
  }

  /* Responsive logic via container queries if placed in a size container */
  @container (max-width: 600px) {
    .spacer {
      /* Example of responsive reduction, assuming a smaller gap token exists */
      --spacer-size: calc(var(--spacer-size) * 0.75);
    }
  }
</style>


