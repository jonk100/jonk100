---
import Code from '../ui/typography/Code.astro';

export interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;
---
<Code inline={false} class={className} {...rest}>
  <slot />
</Code>
